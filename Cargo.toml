[package]
name = "dictionary"
version = "0.1.0"
edition = "2021"

[dependencies]
bzip2 = "0.6.0"
indicatif = "0.18"
quick-xml = "0.38.2"
reqwest = { version = "0.13.4", default-features = false, features = ["blocking", "rustls", "http2"] }
regex = "1.11.1"

[profile.release]
opt-level = 3
lto = true
